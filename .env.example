TELEGRAM_API_ID=
TELEGRAM_API_HASH=

# SSH tunnel for Telegram DC connections (required if Telegram is blocked)
# Uses key auth (ssh_key_ed25519), no username/password needed
# PORT = SSH port on HOST (default: 22)
SOCKS5_HOST=
SOCKS5_PORT=22
